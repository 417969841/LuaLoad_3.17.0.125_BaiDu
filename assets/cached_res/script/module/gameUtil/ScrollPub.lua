module("ScrollPub", package.seeall)


local CCTOUCHBEGAN = "began"
local CCTOUCHMOVED = "moved"
local CCTOUCHENDED = "ended"
local CCTOUCHCANCELED = "cancelled"

--[[--
--触摸监听
--]]
function OnTouchEvent(eventType, x, y)
	if eventType == CCTOUCHBEGAN then

	elseif eventType == CCTOUCHMOVED then

	elseif eventType == CCTOUCHENDED then

	elseif eventType == CCTOUCHCANCELED then

	end
end